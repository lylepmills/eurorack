# Virtual Analog Crossfade

This built-in reference package exposes Emilie Gillet's original
`VA_VARIANT 1` as an independent Plaits Palette model.

The fourth macro scales interval detuning around the original range. The stereo
path keeps the model's full detuned-to-dry-to-sync TIMBRE trajectory active in
both channels.
