# Pulsar

This built-in reference package describes the canonical Rubato Pulsar source.
It exercises all four Plaits Lab controls and provides continuous and triggered
preview scenarios for the local renderer.

The source remains in `plaits/dsp/engine2` while this reference manifest proves
the package contract. Fork tooling will materialize it into package-local
source before a community submission.
